/// Los argumentos que acepta la cafetera.
/// La direccion a un archivo y la direccion del servidor local
pub struct CoffeeArgs {
    pub orders_file_path: String,
    pub server_ip_and_port: String,
}
